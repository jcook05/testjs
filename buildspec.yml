version: 0.2

env:
  variables:
    APP_NAME: profileapp
    BUILD_BRANCH: myArtifactName

phases:
   pre_build: 
     commands:
      
      - echo $CODEBUILD_WEBHOOK_HEAD_REF

      - echo $CODEBUILD_WEBHOOK_HEAD_REF | cut -f3 -d"/"
      
      - BUILD_BRANCH=$(echo $CODEBUILD_WEBHOOK_HEAD_REF | cut -f3 -d"/")
     
   build:
     commands:

artifacts:
  files:
   - public/*
 
  name: $APP_NAME-$BUILD_BRANCH

 ## CODEBUILD_SOURCE_VERSION
 ## CODEBUILD_WEBHOOK_BASE_REF    
 